import { Box } from '@chakra-ui/react'
import React from 'react'

const CardDetails = ({ cardInfo }) => {
    const { cardNumber, expDate, cvv, cardHolderName } = cardInfo
  return (
    <Box>
        
    </Box>
  )
}

export default CardDetails