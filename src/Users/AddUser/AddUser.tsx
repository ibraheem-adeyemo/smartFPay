import React, {useState} from 'react';
import FormModal from '../../shared/components/FormModal/FormModal';

const AddUser = () => {
    return (
        <FormModal title='Add New Admin' primaryButtonLabel='Create Admin' />
    );
}

export default AddUser;